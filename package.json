{
  "name": "batch-watch",
  "version": "0.3.0",
  "description": "Use gulp tasks to watch and compile less, sass, etc, for an entire directory to a single folder",
  "main": "gulpfile.js",
  "scripts": {
    "start": "gulp",
    "clean": "gulp clean:all",
    "clean:dist": "gulp clean:dist",
    "watch": "gulp watch-less"
  },
  "author": "ar-to",
  "license": "ISC",
  "devDependencies": {
    "chokidar": "^1.6.1",
    "del": "^2.2.2",
    "gulp": "^3.9.1",
    "gulp-autoprefixer": "^3.1.1",
    "gulp-less": "^3.3.0",
    "gulp-watch": "^4.3.11",
    "run-sequence": "^1.2.2"
  },
  "dependencies": {
    "config": "^1.26.2"
  }
}
